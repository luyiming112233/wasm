package common

type (
	TypeIdx   = uint32
	FuncIdx   = uint32
	TableIdx  = uint32
	MemIdx    = uint32
	GlobalIdx = uint32
	LocalIdx  = uint32
	LabelIdx  = uint32
)

type FuncType struct {
	// todo
}

type TableType struct {
	// todo
}

type MemType struct {
	// todo
}

type GlobalType struct {
	// todo
}

type Expr struct {
	// todo
}

type ValType struct {
	// todo
}
